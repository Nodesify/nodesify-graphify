

## graphify

This project has an optional nodesify-graphify knowledge graph at `.graphify/`. It is maintained automatically after edits when configured; use `/graphify` for opt-in graph-backed lookup.
